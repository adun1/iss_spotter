const {fetchMyIP} = require('./iss');

const main = function() {
  /*
  fetchMyIP((error, ip) => {
    if (error) {
      console.log("It didn't work!", error);
      return;
    }
  
    console.log("it worked! Returned IP:", ip);
  });
  */

};
main();